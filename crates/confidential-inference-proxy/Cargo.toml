[package]
name = "confidential-inference-proxy"
description = "Optional OpenAI-compatible proxy primitives for the Confidential Inference SDK."
version.workspace = true
edition.workspace = true
license.workspace = true
readme.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
confidential-inference-sdk = { version = "0.1.0", path = "../confidential-inference-sdk" }
confidential-inference-openai = { version = "0.1.0", path = "../confidential-inference-openai" }
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true

[dev-dependencies]
