# Copy to .env and set only the credentialed providers you intend to use.
TINFOIL_API_KEY=
VENICE_API_KEY=
REDPILL_API_KEY=
CHUTES_API_KEY=
NEAR_API_KEY=
PHALA_API_KEY=

# Privatemode is verifier-only in this SDK; its caller-supplied transport owns
# authentication. Demo and fixture-only providers do not require credentials.
